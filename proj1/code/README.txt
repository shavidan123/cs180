Run the code using "python main.py". It should work assuming all relevant images are in the "data" folder (which should be in the same subfolder as "code")

The results will go into the "output" folder.